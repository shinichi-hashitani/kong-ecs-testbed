# Copy to terraform.tfvars and edit. terraform.tfvars is gitignored.

# Restrict ALB ingress. Replace with your office / home IP, e.g. ["203.0.113.10/32"].
allowed_cidrs = [
  "0.0.0.0/0", # CHANGE ME
]
